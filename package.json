{
  "scripts": {
    "build:web": "npx tsc --project src/web/tsconfig.json",
    "build:web:watch": "npx tsc --project src/web/tsconfig.json --watch",
    "start:backend": "python3 run.py",
    "start:all": "npm-run-all --parallel build:web:watch start:backend"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "npm-run-all": "^4.1.5"
  }
}
